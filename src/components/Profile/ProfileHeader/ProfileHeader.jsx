import p from './ProfileHeader.module.scss';

const ProfileHeader = () => {
    return(
        <div className="profile-main__header">
            Profile header
        </div>
    );
}

export default ProfileHeader;